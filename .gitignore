# 示例媒体与字幕(体积大, 不入库; 本地测试用)
spoke.mp4
*.mp4
*.mkv
*.webm
*.avi
*.mov
*.srt
*.ass
*.ssa

# 本地渲染依赖(由 node editor/scripts/fetch-vendor.js 下载, 约 19MB)
editor/vendor/

# 工程数据 / 本地测试脚本 / 截图
.workbuddy/

# main.py 首次运行会在脚本目录写一份本地设置(字体字号等), 不入库
config.json

# 语音识别(创建初稿): Python 环境 / 模型本体(631MB) / 模型目录配置 都在本地生成
asr/.venv/
asr/models/
asr/settings.json
asr/__pycache__/

# whisper.cpp 运行时二进制(约 77MB): 不入库, 首次使用时由服务端自动下载
# (zips→解压→展平到 asr/whisper.cpp/, 见 server.js startRuntimeDownload)
asr/whisper.cpp/

# 本地联调用的视频素材(12 秒英文语音, 供端到端测试)
/_test/

# 其它
node_modules/
Thumbs.db
desktop.ini
.DS_Store
/projects/

# 浏览器兜底通道上传的视频副本(/api/upload-video 存放处)
/videos/

# 发行版打包产物(由 package_release.py 生成) / 解压出来的发行副本
K-ASS-Editor-*.zip
K-ASS-Editor-*/

# SEA 构建产物(build_exe.py 生成: node.exe 副本 175MB + blob + 注入后的 exe)
/build/


# Python 字节码
__pycache__/
